<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <p>Hola bienvenido a mi API de MONEDERO by Codigo</p>
    <p>Presentado por {{ nombre}}</p>
    <p>Los galacticos son:</p>
    <ol>
    {% for integrante in integrantes %}
    <li>{{integrante}}</li>
    {% endfor %} 
    </ol>
    {{integrantes}}
    <p>El usuario es {{usuario.nombre}}</p>

    <table border="1">
        <tr>
          <th>seleccion</th>
          <th>clasificado</th>
        </tr>
      
        {% for seleccion in selecciones %}
        <tr>
            <td>{{seleccion.nombre}}</td>
            <td>
                {{seleccion.clasificado}}
                {% if seleccion.clasificado == true %}
                'Nos vemos en Qatar'
                {% elif seleccion.clasificado is false %}
                'Con suerte en el proximo'
                {% else %}
                'Aun no se sabe'
                {% endif %}
            </td>
        </tr>
        {% endfor %} 

      </table>

</body>
</html>